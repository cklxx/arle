[package]
name = "tools"
version = "0.1.0"
edition = "2024"

[dependencies]
chat = { path = "../chat" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
