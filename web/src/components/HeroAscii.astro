---
interface Props {
  ariaLabel: string;
}
const { ariaLabel } = Astro.props;
---
<div class="wordmark" aria-label={ariaLabel}>
  <span class="wordmark-name">arle</span><span class="wordmark-section">(1)</span>
  <span class="wordmark-rule" aria-hidden="true"></span>
</div>
